$(document).ready(function() {
    var botao = $('.bt-1');
    var dropDown = $('.ul-home');    
   
       botao.on('click', function(event){
           dropDown.stop(true,true).slideToggle();
           event.stopPropagation();
       });
   });
   //Subtituir as 2 classes
   $(document).ready(function() {
    var botao = $('.bt-2');
    var dropDown = $('.ul-pizza');    
   
       botao.on('click', function(event){
           dropDown.stop(true,true).slideToggle();
           event.stopPropagation();
       });
   });
   //Subtituir as 2 classes
   
   $(document).ready(function() {
    var botao = $('.bt-3');
    var dropDown = $('.ul-sobre');    
   
       botao.on('click', function(event){
           dropDown.stop(true,true).slideToggle();
           event.stopPropagation();
       });
   });
   //Subtituir as 2 classes
   $(document).ready(function() {
    var botao = $('.bt-4');
    var dropDown = $('.ul-endereco');    
   
       botao.on('click', function(event){
           dropDown.stop(true,true).slideToggle();
           event.stopPropagation();
       });
   });
   //Subtituir as 2 classes
         